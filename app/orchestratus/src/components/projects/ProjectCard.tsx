import { Badge, Button, Card, Group, Stack, Text } from '@mantine/core'
import { STATUS_LABELS, type Project } from '../../types'
import { statusBadgeColors } from '../shared'
export function ProjectCard({ project, onEdit, onDelete }: { project: Project; onEdit: (project: Project) => void; onDelete: (project: Project) => void }) {
  const color = project.Color || '#4c6ef5'
  return <Card className="project-card" withBorder padding="lg" style={{ borderLeft: '3px solid transparent', borderImage: `linear-gradient(to bottom, ${color}, transparent) 1`, backgroundImage: `radial-gradient(120% 90% at 0% 0%, ${color}26 0%, transparent 55%)` }}><Group justify="space-between" align="flex-start" wrap="nowrap"><Stack gap={4} className="project-card-heading"><Text className="project-card-kicker" size="xs" tt="uppercase" style={{ color }}>Project #{project.ID}</Text><Text className="project-card-title" fw={650} c="gray.0">{project.Title}</Text></Stack><Badge color={statusBadgeColors[project.Status]} variant="light">{STATUS_LABELS[project.Status]}</Badge></Group><Text className={`project-card-description${project.Description ? '' : ' project-card-description-empty'}`} size="sm" c={project.Description ? 'gray.5' : 'dimmed'} mt="md">{project.Description || 'No description provided.'}</Text><Group className="project-card-footer" justify="space-between" align="center" mt="lg" pt="sm" gap="sm" wrap="nowrap"><Text size="xs" c="dimmed">Created {new Date(project.DateCreated).toLocaleDateString()}</Text><Group gap="xs" wrap="nowrap"><Button size="xs" radius="xl" variant="default" onClick={() => onEdit(project)}>Edit</Button><Button size="xs" radius="xl" color="red" variant="light" onClick={() => onDelete(project)}>Delete</Button></Group></Group></Card>
}
